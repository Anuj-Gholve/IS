# AES-128 Algorithm - Pure Python (No Libraries)

# S-Box
SBOX = [
    
]

# Inverse S-Box
INV_SBOX = [0] * 256
for i in range(256):
    INV_SBOX[SBOX[i]] = i

# Round constants
RCON = [0x01,0x02,0x04,0x08,0x10,0x20,0x40,0x80,0x1b,0x36]

def sub_bytes(state):
    return [SBOX[b] for b in state]

def inv_sub_bytes(state):
    return [INV_SBOX[b] for b in state]

def shift_rows(s):
    return [s[0],s[5],s[10],s[15], s[4],s[9],s[14],s[3],
            s[8],s[13],s[2],s[7],  s[12],s[1],s[6],s[11]]

def inv_shift_rows(s):
    return [s[0],s[13],s[10],s[7], s[4],s[1],s[14],s[11],
            s[8],s[5],s[2],s[15],  s[12],s[9],s[6],s[3]]

def xtime(a):
    return ((a << 1) ^ 0x1b) & 0xff if a & 0x80 else (a << 1) & 0xff

def mix_single(a, b, c, d):
    return xtime(a)^xtime(b)^b^c^d, a^xtime(b)^xtime(c)^c^d, a^b^xtime(c)^xtime(d)^d, xtime(a)^a^b^c^xtime(d)

def mix_columns(s):
    r = list(s)
    for i in range(4):
        c = i * 4
        r[c], r[c+1], r[c+2], r[c+3] = mix_single(s[c], s[c+1], s[c+2], s[c+3])
    return r

def gmul(a, b):
    p = 0
    for _ in range(8):
        if b & 1: p ^= a
        hi = a & 0x80
        a = (a << 1) & 0xff
        if hi: a ^= 0x1b
        b >>= 1
    return p

def inv_mix_columns(s):
    r = list(s)
    for i in range(4):
        c = i * 4
        a, b, cc, d = s[c], s[c+1], s[c+2], s[c+3]
        r[c]   = gmul(a,14) ^ gmul(b,11) ^ gmul(cc,13) ^ gmul(d,9)
        r[c+1] = gmul(a,9)  ^ gmul(b,14) ^ gmul(cc,11) ^ gmul(d,13)
        r[c+2] = gmul(a,13) ^ gmul(b,9)  ^ gmul(cc,14) ^ gmul(d,11)
        r[c+3] = gmul(a,11) ^ gmul(b,13) ^ gmul(cc,9)  ^ gmul(d,14)
    return r

def add_round_key(state, key):
    return [s ^ k for s, k in zip(state, key)]

def key_expansion(key):
    expanded = list(key)
    for i in range(4, 44):
        temp = expanded[(i-1)*4:i*4]
        if i % 4 == 0:
            temp = temp[1:] + temp[:1]           # RotWord
            temp = [SBOX[b] for b in temp]       # SubWord
            temp[0] ^= RCON[i // 4 - 1]
        word = expanded[(i-4)*4:(i-3)*4]
        expanded += [w ^ t for w, t in zip(word, temp)]
    return expanded

def aes_encrypt(plain, key):
    keys = key_expansion(key)
    state = add_round_key(plain, keys[0:16])
    for r in range(1, 10):
        state = sub_bytes(state)
        state = shift_rows(state)
        state = mix_columns(state)
        state = add_round_key(state, keys[r*16:(r+1)*16])
    state = sub_bytes(state)
    state = shift_rows(state)
    state = add_round_key(state, keys[160:176])
    return state

def aes_decrypt(cipher, key):
    keys = key_expansion(key)
    state = add_round_key(cipher, keys[160:176])
    for r in range(9, 0, -1):
        state = inv_shift_rows(state)
        state = inv_sub_bytes(state)
        state = add_round_key(state, keys[r*16:(r+1)*16])
        state = inv_mix_columns(state)
    state = inv_shift_rows(state)
    state = inv_sub_bytes(state)
    state = add_round_key(state, keys[0:16])
    return state

def text_to_bytes(text, size=16):
    b = [ord(c) for c in text]
    return (b + [0] * size)[:size]

def bytes_to_hex(b):
    return ''.join(format(x, '02x') for x in b)

def bytes_to_text(b):
    return ''.join(chr(x) for x in b if x != 0)

# --- Main ---
plain_text = input("Enter message (up to 16 chars): ")
key_text = input("Enter key (16 chars): ")

plain = text_to_bytes(plain_text)
key = text_to_bytes(key_text)

encrypted = aes_encrypt(plain, key)
print("Encrypted (hex):", bytes_to_hex(encrypted))

decrypted = aes_decrypt(encrypted, key)
print("Decrypted:", bytes_to_text(decrypted))
